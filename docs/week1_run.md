# Week 1 Run Log

## Proof of execution

### Command
```bash
cd src
source ../.venv/bin/activate
python hello_llm.py "What is RAG ?"
```

### Output
```text
(.venv) sanjanabharti@Amits-MacBook-Pro src % python hello_llm.py "What is RAG ?"
RAG stands for Retrieval-Augmented Generation. It is a framework that combines retrieval-based methods with generative models. In RAG, a system retrieves relevant documents from a large dataset and then uses a generative model, like a language model, to produce coherent and contextually relevant responses based on the retrieved information. This approach enhances the quality and relevance of the generated content, particularly in tasks like question answering and conversational AI.
```

### Command
```bash
python hello_llm.py "What Day is Tuday  ?"
```

### Output
"Tuday" is not a recognized weekday. If you meant "Tuesday," it is the second day of the week, following Monday. If you have a specific date in mind, please provide it for clarification.
